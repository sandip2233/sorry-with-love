import './Promise.css';

const Promise = () => {
  return (
    <section className="promise-component" aria-label="Promise component">
      <h2>Promise</h2>
    </section>
  );
};

export default Promise;
